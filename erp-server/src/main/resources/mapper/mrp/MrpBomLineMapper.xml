<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.modules.mrp.mapper.MrpBomLineMapper">
    <select id="selectPageByQuery" resultType="com.erp.server.modules.mrp.entity.MrpBomLine">
        SELECT id,
        name,
        bom_id,
        product_id,
        product_qty,
        company_id FROM mrp_bom_line
        <where>
            <if test="query.bomId != null">
                AND bom_id = #{query.bomId}
            </if>
            <if test="query.productId != null">
                AND product_id = #{query.productId}
            </if>
            <if test="query.keyword != null and query.keyword != ''">
                AND (name ILIKE CONCAT('%', #{query.keyword}, '%'))
            </if>
        </where>
        ORDER BY id DESC
    </select>
</mapper>
